# Codecov configuration
# https://docs.codecov.com/docs/codecov-yaml

coverage:
  # Badge color thresholds: 60% = red, 90% = green
  range: "60...90"
  status:
    project:
      default:
        # Overall project coverage target
        target: auto  # Use coverage from base commit
        threshold: 5%  # Allow 5% drop
    patch:
      default:
        target: 80%
        informational: true  # Report but don't block (CLI entrypoints have low patch coverage)

comment:
  layout: "reach,diff,files"
  behavior: default
  require_changes: true  # Only comment when coverage changes
